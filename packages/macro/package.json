{
  "name": "@organic/macro",
  "version": "0.1.0",
  "description": "Macro expansion system for conversation requests - supports expressions to insert system prompts, files, env vars, and conversation history",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@organic/utils": "workspace:*",
    "@organic/agent": "workspace:*",
    "@organic/tools": "workspace:*",
    "chalk": "^5.4.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
